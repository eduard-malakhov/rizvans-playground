import { createElement } from 'react';
import ReactDOM from 'react-dom';
import { Application } from './components/Application';

ReactDOM.render(createElement(Application), document.getElementById('root'));
